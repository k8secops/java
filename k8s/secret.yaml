apiVersion: v1
kind: Secret
metadata:
  name: gitops-app-secret
  namespace: gitops-app
  labels:
    app.kubernetes.io/name: gitops-app
type: Opaque
# Values are base64-encoded. Replace with real credentials before deploying.
# echo -n 'yourvalue' | base64
data:
  DB_USERNAME: c2E=        # sa
  DB_PASSWORD: cGFzc3dvcmQ=  # password
