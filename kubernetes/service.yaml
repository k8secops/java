apiVersion: v1
kind: Service
metadata:
  name: java-inventory
  namespace: demo
  labels:
    app: java-inventory
spec:
  type: ClusterIP
  selector:
    app: java-inventory
  ports:
    - port: 8080
      targetPort: 8080
